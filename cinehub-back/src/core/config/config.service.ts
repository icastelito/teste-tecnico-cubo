/**
 * ConfigService customizado não é necessário.
 * Usar diretamente o ConfigModule e ConfigService do @nestjs/config
 * que já está configurado globalmente no AppModule.
 *
 * Este arquivo pode ser deletado.
 */
